代码基于python==3.8.0
torch==1.8.0 
torchvision==0.9.0
CUDA=11.1

Exercise3和Exercise4报告写在了一块，
在实验三报告里

Exercise2和Exercise3的models文件夹用于存放训练后的模型用于特征可视化，nets文件夹用于存放训练后转换的ONNX模型用于结构可视化，figures文件夹是误差和准确度曲线。

